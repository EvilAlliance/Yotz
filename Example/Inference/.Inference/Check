:i argc 0
:b stdin 0

:i returncode 0
:b stdout 188
main: () u8 #inferedFromExpression : () u8{ 
    a: u8 #inferedFromUseu16 #inferedFromUse : 10;
    b: u16 = 30;
    c: u16 #inferedFromExpression = ((a + b) + 30);
    return (a + 10);
}

:b stderr 0

